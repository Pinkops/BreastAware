# ---- Client settings (these are PUBLIC by design — they ship to the browser) ----
VITE_SUPABASE_URL=https://YOUR-PROJECT-REF.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_xxx

# ---- Server-only settings (SECRET — real values live in Vercel, never in Git) ----
NEXT_PUBLIC_SUPABASE_URL=https://YOUR-PROJECT-REF.supabase.co
SUPABASE_SERVICE_ROLE_KEY=

# NOTE: Google sign-in vars and FULLSTACK_* platform vars are removed
# (Google button gets disabled in Step 8; platform leftovers deleted in Step 7)
